export const environment = {
  production: true,
  // Update this with your platform-server URL (REST API)
  // If hosted on Render, it should be something like: https://your-platform-server.onrender.com/api
  apiUrl: 'https://blog-plateform-platform-server.onrender.com/api',
  // WebSocket server URL (for Socket.IO)
  socketUrl: 'https://blog-plateform-websocket-server.onrender.com'
};

